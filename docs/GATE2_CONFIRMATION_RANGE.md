# Gate 2 confirmation range

Frozen before Gate-2 held-out execution:

```text
seed_start = 280000
seed_count = 12
```

The confirmation uses the thresholds in `docs/GATE2_GRAPH_ATTACKER_PREREG.md`.

If a code defect invalidates this range, do not silently reuse the same seeds for a revised protocol.
