# Gate 2 branch note

Gate 2 is preregistered in `docs/GATE2_GRAPH_ATTACKER_PREREG.md`.

Active implementation branch should be `sol/gate2-abstract-graph-attacker`.

Do not alter confirmation thresholds after inspecting held-out confirmation results. If implementation bugs require a protocol change, record the old result as invalid and preregister a new version before rerunning fresh seeds.
