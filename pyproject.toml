[project]
name = "sunday-microarbor"
version = "0.0.1"
description = "Falsification-driven structural relational memory experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
plot = ["matplotlib>=3.7"]
